
check consistency: 
- SNP names: show observed alleles if mismath / complemented/ ...
- remove ambiguous

Verify that effect allele if correctly selected no matter how in data

Revise 
        Percentile ranks
        standardization of score
        
Allow variable / no header in metadata

save PGS to data

clean:

Hide empty tables if not selected or empty because no data

Test in criprev

Replace individual ID by adjustment covariates for the regression models
Revise association table
	log-OR -> OR
	SE for t-test
	Value ?
	 
          add_row(paste0("Welch t-test (", lbl, ")"),
                  "t", diff(tt$estimate), tt$stderr,
                  tt$conf.int[1], tt$conf.int[2],
                  tt$statistic, round(tt$parameter, 1), tt$p.value)
                  
                  
                  
                  
SNPs 
assoc/haplotypes
if value > 1000 : Inf
NA : ''